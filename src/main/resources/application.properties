spring.application.name=hng4

#  spring.datasource.url=jdbc:postgresql://localhost:5432/flexisaf
#  spring.datasource.username=postgres
#  spring.datasource.password=root

 spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=require
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect



# typesense.api-key=W5bFgG7fWZA3DHhPNbA71JhyPxtycWJA
# typesense.node=9fiu6e3an7zh8cmkp-1.a1.typesense.net:443


typesense.api-key=${TYPESENSE_API_KEY}
typesense.node=${TYPESENSE_NODE}

# Configure datasource using JDBC URI provided by Koyeb
# spring.datasource.url=${JDBC_URI}
# spring.datasource.username=${DATABASE_USERNAME}
# spring.datasource.password=${DATABASE_PASSWORD}

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT secret and expiration configured as environment variables
# app.typesense-api-key=${TYP_KEY}
# app.jtypesense.nodes=${TYP_NODE}

#  typesense.api-key=L1qv6eI5IWEQUWYuAteABdYOapQ9imfu
#  typesense.nodes=9fiu6e3an7zh8cmkp-1.a1.typesense.net:443
